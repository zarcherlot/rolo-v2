<!-- status: active; authority: reference; owner: rolo maintainers; last_reviewed: 2026-09-09; last_synced_commit: 390bad38ce0d84f4772d203423929e2a0ebc4a50 -->

# Rolo v2 工程状态

本台账记录 Probe-first 产品链以及当前旋转 MVP 的 Trace/Certify 增量；Trace/Certify 的
完整真机和 Web UI 发布闭环仍未完成。
`STABLE` 表示边界和拒绝路径有测试，`PARTIAL` 表示仍需真实目标机或平台证据，`DRAFT`
表示仅有设计。证据等级：E0 文档、E1 单测、E2 离线闭环、E3 固定目标机、E4 真机闭环。

| feature_id | maturity | evidence | user surface | code_paths | test_paths | known limits |
|---|---|---|---|---|---|---|
| FEAT-PROBE-ENROLLMENT | STABLE | E4 | `rolo target profile`; `rolo target inspect-profile` | `src/rolo/targets/profiles.py`; `src/rolo/targets/credentials.py`; `src/rolo/targets/executor.py` | `tests/test_target_credentials.py`; `tests/test_target_executors.py`; `tests/test_product_cli_v2.py` | 需要用户首次批准 host key；密码只用于一次性置备，运行时不接受密码 |
| FEAT-PROBE-EVIDENCE | STABLE | E4 | `rolo probe`; `robotctl probe start` | `src/rolo/stages/probe/target_evidence.py`; `src/rolo/stages/probe/active_discovery.py`; `src/rolo/stages/probe/discovery.py` | `tests/test_probe_evidence_contract.py`; `tests/test_product_cli_v2.py` | Evidence 是采集时刻事实；可携带签名 source snapshot，但不证明物理安全或行为正确性 |
| FEAT-PROBE-NATIVE-SURFACE | STABLE | E4 | `rolo target tool-surface --profile` | `src/rolo/agent_tools/native_tools.py`; `src/rolo/agent_tools/session_factory.py` | `tests/test_agent_native_tools.py`; `tests/test_probe_session_factory.py` | 当前为四类 family 的只读 surface；缺失命令返回 UNAVAILABLE |
| FEAT-PROBE-TOOL-PLAN | STABLE | E4 | `rolo target tool-plan --profile PLAN.json` | `src/rolo/agent_tools/planning.py`; `src/rolo/agent_tools/session.py` | `tests/test_tool_planning.py`; `tests/test_native_tool_session.py`; `tests/test_product_cli_v2.py` | Agent 只能规划，Rolo 校验 digest、目标、allowlist、预算和只读模式 |
| FEAT-PROBE-SSH-RUNNER | STABLE | E4 | profile-bound remote Native Tool execution | `src/rolo/targets/executor.py`; `src/rolo/agent_tools/native_tools.py` | `tests/test_target_executors.py`; `tests/test_agent_native_tools.py`; `tests/test_product_cli_v2.py` | Provider 可能依赖目标 OS/Middleware setup、Python packages 和 runtime libraries；环境不全时明确失败 |
| FEAT-PROBE-CONFORMANCE | STABLE | E4 | Tool Surface / ToolPlan conformance artifacts | `src/rolo/agent_tools/conformance.py`; `src/rolo/product_cli.py` | `tests/test_tool_conformance.py`; `tests/test_native_tool_session.py`; `tests/test_product_cli_v2.py` | Conformance 只固化当前 Tool Surface；不声明 Trace/Certify 或 release authority |
| FEAT-MVP-ROTATION-TRACE | PARTIAL | E3 | `app.base.rotate` supervised field execution | `src/rolo/mvp/trace.py`; `src/rolo/mvp/ros_binding.py`; `src/rolo/mvp/bounded_twist.py`; `src/rolo/mvp/trace_diagnostics.py`; `src/rolo/targetd/worker.py`; `src/rolo/product_cli.py`; `docs/validation/LANDERPI_BOUNDED_TWIST_CANARY_20260907.json`; `docs/validation/LANDERPI_TARGETD_PROVIDER_CANARY_20260907.json` | `tests/test_mvp_rotation.py`; `tests/test_mvp_rotation_execution.py`; `tests/test_trace_diagnostics.py`; `tests/test_ros_binding.py`; `tests/test_targetd_protocol.py` | 已用隔离 `/cmd_vel` 完成一次真机受监督 1° bounded canary，并通过完整 targetd bundle/provider journey：独立三路 IMU 精度、零速停止和 settle 通过；共享 `/controller/cmd_vel` 仍需显式 route + source confirmation。DSL compiler/release/catalog 生成 bundle 与 10-case Certify/Trace 的完整现场旅程仍未闭合 |
| FEAT-MVP-CERTIFY | PARTIAL | E3 | explicit Certify request、10-case suite/report、release-bound targetd runner | `src/rolo/mvp/certify.py`; `src/rolo/mvp/contracts.py`; `src/rolo/mvp/http.py`; `src/rolo/releases/journey.py`; `src/rolo/targetd/certify_adapter.py`; `schemas/CertificationSuite.schema.json`; `schemas/CertificationReport.schema.json`; `schemas/TargetdV2CertifyReceiptSidecar.schema.json`; `docs/validation/landerpi_n6_formal_r0_20260909/` | `tests/test_mvp_journey.py`; `tests/test_targetd_certify_adapter.py`; `tests/test_landerpi_n7_r0_canary.py` | N6 的 fixed-target R0 正式链路已达 E3：LanderPi 同一 pinned session 由 `ReleaseBoundCertify` 驱动 10/10 targetd CALL，每例持久化不可覆盖 v2 CallReceipt sidecar，并生成 report/events/release-binding/artifact index；post-cancel provider count 不增。证据仍使用 fixture operator/partial authority，且仅覆盖 `/odom` OBSERVE/read/R0，不能外推为生产身份或实体运动验收 |
| FEAT-MVP-HARNESS-CODEGEN | PARTIAL | E2 | descriptor-driven Harness function generation | `src/rolo/mvp/harness_codegen.py`; `skills/rolo-harness-codegen/` | `tests/test_harness_codegen.py`; `tests/test_harness_codegen_skill.py` | 已生成通用输入/输出校验 wrapper，registration proposal 可携带 codegen contract；目标机 artifact 持久化与 Trace 重建仍待完成 |
| FEAT-APPLICATION-GAP-BUNDLE | STABLE | E4 | `rolo target application-bundle --profile --application` | `src/rolo/stages/probe/application.py`; `src/rolo/product_cli.py` | `tests/test_application_bundles.py` | 非旋转应用能力仍按 startup/navigation/mapping/manipulation route-level candidate 处理；route presence 不证明应用行为正确；无证据时 Conformance 明确失败 |
| FEAT-APPLICATION-OPERATION-SLICE | STABLE | E4 | `rolo target application-operation --profile --operation` | `src/rolo/stages/probe/application.py`; `src/rolo/product_cli.py` | `tests/test_application_bundles.py` | v1 137 项中先实现 32 个只读 route-binding rules；当前 bundle 是 route-level candidate，不等同于行为/结果验证；R2/R3 明确 DEFERRED |
| FEAT-RKB-EVIDENCE-ENVELOPE | PARTIAL | E4 | typed read-only envelope API | `src/rolo/rkb/models.py`; `src/rolo/rkb/canonical.py`; `src/rolo/rkb/migration.py`; `src/rolo/rkb/validation.py`; `src/rolo/stages/probe/target_evidence.py`; `schemas/RobotSnapshot.schema.json` | `tests/test_rkb_envelope.py`; `tests/test_rkb_contract_baseline.py`; `tests/test_rkb_compatibility.py` | RKB-1 已覆盖 identity/digest/freshness/source/access、HMAC 前置校验、v2/v3 兼容迁移与 DiscoveryReport 投影；开发机 74 passed/1 skipped，目标机 75 passed；尚无真机 Episode 持久化 |
| FEAT-RKB-TYPED-READ-MODELS | PARTIAL | E3 | `robot.identity`; `os.runtime.status`; `hw.inventory.scan`; `middleware.*`; `app.executable.inspect`; `capability.get`; `state_safety.snapshot` | `src/rolo/rkb/read_models.py`; `src/rolo/rkb/query.py`; `scripts/rkb2_landerpi_smoke.py` | `tests/test_rkb_typed_queries.py`; RKB-1 compatibility tests; LanderPi smoke | 固定 LanderPi `mentorpi` 已完成 fresh probe runner VERIFIED → bundle → snapshot → typed query `--live`（exit 0）；旧 Bundle 的 payload hash mismatch 仍 fail-closed；query 对 digest/fingerprint/freshness fail-closed，硬件枚举受目标机能力限制，middleware graph 与 state safety 仍保守为 UNKNOWN/观察投影 |
| FEAT-MHS-READONLY-PROVIDER | PARTIAL | E1 | MHS inspect/status/read SPI | `src/rolo/mhs_hardware.py`; `examples/mhs-sensor/` | `tests/test_mhs_hardware.py`; `examples/mhs-sensor/test_mhs_sensor.py` | 仅 fake backend；写能力（reset/calibrate/setpoint/power）不属于当前 v2 |
| FEAT-RKB-0-BASELINE | STABLE | E1 | RKB 契约与依赖验收 | `docs/architecture/RKB_CONTRACT_DECISIONS_ZH.md`; `docs/reference/RKB_IMPLEMENTATION_MAP_ZH.md`; `docs/reference/RKB_DEPENDENCIES_ZH.md` | `tests/test_rkb_contract_baseline.py` | RKB-0 契约、schema、依赖入口和拒绝路径已冻结；真机 canary 与后续 read model 属于 RKB-1/RKB-3，尚未开始 |
| FEAT-DSL-COMPILER-STANDALONE | STABLE | E2 | `rolo-dsl check`; receipt-gated `rolo-dsl compile` | `src/rolo/dsl/`; `pyproject.toml`; `docs/validation/DSL_COMPILER_G7_RELEASE_20260906.md` | `tests/test_dsl_cli.py`; `tests/test_dsl_api.py`; `tests/test_dsl_service.py`; `tests/test_dsl_service_digest.py`; `tests/test_dsl_compiler_context.py`; `tests/test_dsl_conformance_report.py` | 公共 `compile_document`/`compile_text` 与 request v2 均要求 committed active Mapping receipt；后端只写同盘 staging，完成独立 manifest/artifact 校验后再以 admission ledger 同锁原子提交，取消或过期不留下可见 Bundle。Compiler 仍只生成离线 IR/Bundle，目标 runtime 绑定和目标侧执行围栏由 targetd 负责 |
| FEAT-DSL-CONTRACT-HANDOFF | PARTIAL | E2 | versioned contract manifest、canonicalize/replay、typed BundlePlan v2、targetd DSL frames | `src/rolo/dsl/contracts.py`; `src/rolo/dsl/canonical.py`; `src/rolo/dsl/bundle_plan.py`; `src/rolo/dsl/diagnostics.py`; `src/rolo/dsl/backends/fake.py`; `src/rolo/targetd/dsl_service.py`; `schemas/rolo-dsl/v1/contract-manifest.json`; `schemas/rolo-dsl/v1/bundle-plan.json` | `tests/test_dsl_bundle_plan.py`; `tests/test_dsl_contract.py`; `tests/test_dsl_contract_matrix.py`; `tests/test_dsl_execute_contract.py`; `tests/test_dsl_cli.py`; `tests/test_targetd_dsl_service.py`; `tests/test_release_journey.py` | BundlePlan v2 与 fake Backend SPI v2 已在 compiler 绑定 DSL/Context/IR/backend/artifact identity；targetd 会独立同进程编译，Journey 比较 controller/targetd Bundle digest。targetd compile 与 Target Conformance 的 `schema_version` 已必填且 v1/缺失均拒绝；backend registry 的严格 SPI 版本协商、其余契约的 missing-version 审计和跨语言 canonical fixture 仍未完成 |
| FEAT-DSL-CONTEXT-ADAPTER | PARTIAL | E2 | Probe evidence → Compile Context | `src/rolo/dsl/context_adapter.py`; `src/rolo/dsl/context.py` | `tests/test_dsl_context_adapter.py` | 当前投影 routes、schemas、MHS、freshness、warnings/errors；RKB 和已发布 Tool Catalog 的完整联合投影仍需集成 |
| FEAT-TARGETD-DSL-PIPELINE | PARTIAL | E3 | receipt-gated DSL frames、runtime resolution、verified Release/authority 与 generic CALL | `src/rolo/targetd/dsl_protocol.py`; `src/rolo/targetd/dsl_service.py`; `src/rolo/targetd/transport.py`; `src/rolo/targetd/protocol.py`; `src/rolo/targetd/service.py`; `src/rolo/targetd/daemon.py`; `src/rolo/targetd/ros2_runtime.py`; `src/rolo/targetd/runtime_backend.py`; `scripts/landerpi_n7_r0_canary.py`; `docs/validation/landerpi_n6_formal_r0_20260909/` | `tests/test_targetd_dsl_service.py`; `tests/test_targetd_ros2_runtime.py`; `tests/test_targetd_protocol.py`; `tests/test_targetd_certify_adapter.py`; `tests/test_landerpi_n7_r0_canary.py` | E3 仅限 fixed-target `OBSERVE/read/R0` 与 fixture authority：fresh `/odom` snapshot、DSL 五阶段、T1～T4、target-owned Release/authority、Trace 1 和正式 Certify 10 已在一条 pinned SSH stdio channel 闭合。response sequence/run/request/call identity 错误会 sticky-poison client 且不重发。DSL PUT durable resume、通用 Bundle bridge、生产 authority adapter、process seam 的 service/daemon 接线、非 R0 worker 和 authenticated duplex control 仍缺 |
| FEAT-TARGETD-SESSION-RECOVERY | PARTIAL | E2 | signed-bundle session/receipt persistence、replay guard、worker lifecycle/process substrate | `src/rolo/targetd/protocol.py`; `src/rolo/targetd/service.py`; `src/rolo/targetd/daemon.py`; `src/rolo/targetd/lifecycle.py`; `src/rolo/targetd/lifecycle_integration.py`; `src/rolo/targetd/process_worker.py` | `tests/test_targetd_protocol.py`; `tests/test_targetd_ssh_handoff.py`; `tests/test_targetd_worker_lifecycle.py`; `tests/test_targetd_worker_integration.py`; `tests/test_targetd_process_worker.py` | Durable lease/heartbeat、唯一 supervisor incarnation、exact call-key interrupt/ack、deadline/clock rollback/crash/restart/replay reconciliation 已实现；opt-in spawn-process seam 只允许 exact `/odom` R0，并用有界 JSON duplex IPC 与 WORK_STARTED 门处理实时 CANCEL/STOP。它未接 service/default daemon，且缺 target-owned START/live fence CAS、production adapter、OS sandbox 和 durable result/receipt commit，所以仍不是 production worker 或实体停止证据 |
| FEAT-TARGETD-MOTION-SAFETY | PARTIAL | E2 | physical-motion admission model and protocol fence | `src/rolo/targetd/motion_safety.py`; `src/rolo/targetd/protocol.py`; `src/rolo/targetd/service.py`; `schemas/TargetdMotionSafetyAdmission.schema.json`; `schemas/ExecutionRequestV3.schema.json` | `tests/test_targetd_motion_safety.py`; `tests/test_targetd_protocol.py` | 离线 evaluator 严格绑定 operator、现场 presence、安全区、独立急停、实时 ROS graph、命令/direct-motor publisher、motor fence 与 target stop acknowledgement；v2/v1 migration 的 physical request 均拒绝。现有 OS-process/duplex 只读 seam 未接 TargetdService，也没有 provider 临界点实时 graph/fence CAS；生产 service 即使收到完整 synthetic v3 evidence 也无条件在 provider 前拒绝，不能执行实体运动 |
| FEAT-TOOL-RELEASE-LIFECYCLE | PARTIAL | E2 | append-only transactional Catalog、target signature receipt、resumable upload | `src/rolo/releases/publisher.py`; `src/rolo/releases/catalog.py`; `src/rolo/releases/signature.py`; `src/rolo/releases/upload.py`; `src/rolo/releases/consumers.py`; `src/rolo/targetd/service.py` | `tests/test_dsl_release.py`; `tests/test_release_lifecycle.py`; `tests/test_release_catalog_transaction.py`; `tests/test_release_chunk_upload.py`; `tests/test_dsl_consumers.py`; `tests/test_targetd_protocol.py` | Catalog 已有跨进程 CAS、append-only transaction log、signature-required mode、日志推导/校验 snapshot、crash-tail recovery、前进式 rollback、no-replace publish 和不可按 mtime 偷取的事务锁；上传支持 32 MiB/1 MiB×64 内容寻址分片恢复/篡改检测。compatibility ReleasePublisher 仍显式 unsigned；真实 target signer/trust provider、可信 artifact reload、rollback/current authority/freshness、跨主机 head、Windows 目录 fsync 与 targetd 通用上传接线仍未部署 |
| FEAT-POST-COMPILER-JOURNEY | PARTIAL | E3 | receipt-bound DSL→targetd→Release→Trace/Certify replay | `src/rolo/releases/journey.py`; `src/rolo/targetd/certify_adapter.py`; `src/rolo/observability.py`; `schemas/rolo-dsl/v1/post-compiler-journey-result.json`; `scripts/landerpi_n7_r0_canary.py`; `docs/validation/landerpi_n6_formal_r0_20260909/` | `tests/test_release_journey.py`; `tests/test_targetd_certify_adapter.py`; `tests/test_observability.py`; `tests/test_landerpi_n7_r0_canary.py` | fixed-target R0 已由正式 `ReleaseBoundCertify` 生成 10 个 receipt sidecar、events、report 与 index，并与同 session 的 Release/Context/target/current authority 交叉验证。通用 `PostCompilerJourney` 的 target transport 接线、durable resume、Episode/list/download/history 与正式 ReleaseBoundTrace 仍为 E2/未闭合 |
| FEAT-POST-COMPILER-OBSERVABILITY | PARTIAL | E2 | journey metrics、日志脱敏、artifact retention | `src/rolo/observability.py`; `schemas/rolo-dsl/v1/journey-metric.json` | `tests/test_observability.py` | 已支持固定指标 schema、敏感键脱敏、单文件滚动和目录内显式清理；现场 metrics backend 与 acceptance pack 仍待接入 |
| FEAT-TARGETD-FIELD-RUNBOOK | PARTIAL | E2 | targetd 安装/升级/health、ROS2 bringup 前置、故障分类和回滚 | `src/rolo/targetd/installer.py`; `docs/validation/TARGETD_FIELD_RELEASE_RUNBOOK_ZH.md` | `tests/test_targetd_ssh_handoff.py` | 离线安装清单、幂等升级和确认卸载已覆盖；真实现场升级/回滚 acceptance 仍待执行 |
| FEAT-RELEASE-COMPATIBILITY-POLICY | PARTIAL | E2 | 发布版本、兼容窗口、STALE 和回滚策略 | `src/rolo/releases/publisher.py`; `docs/architecture/ROLO_V2_RELEASE_COMPATIBILITY_POLICY_ZH.md` | `tests/test_release_lifecycle.py`; `tests/test_dsl_consumers.py` | schema/digest 兼容边界和离线回滚已固定；跨版本真实目标升级 acceptance 仍待执行 |
| FEAT-CAPABILITY-CANDIDATE-INDEX | PARTIAL | E2 | observed Context → Candidate Index → intent query | `src/rolo/dsl/candidates.py`; `schemas/rolo-dsl/v1/capability-candidate-index.json` | `tests/test_dsl_candidates.py` | 候选只来自 observed route/Tool 且保留缺口；Bootstrap Probe 的现场生成和 rolo-vis 展示仍待接入 |
| FEAT-BOOTSTRAP-ARTIFACT-REPLAY | PARTIAL | E1 | Bootstrap artifact verify/replay；`rolo-dsl bootstrap-verify` | `src/rolo/dsl/bootstrap_replay.py`; `schemas/rolo-dsl/v1/bootstrap-replay-report.json`; `src/rolo/dsl/cli.py` | `tests/test_dsl_bootstrap_replay.py`; `tests/test_dsl_cli.py` | 只读校验 Context、Candidate Index、artifact index 和 target/identity digest；真实目标 Bootstrap 与生产签名仍待现场接入 |
| FEAT-MAPPING-PROPOSAL | PARTIAL | E2 | digest-bound proposal evidence/risk/unknowns | `src/rolo/dsl/proposal.py`; `schemas/rolo-dsl/v1/mapping-proposal.json` | `tests/test_dsl_proposal.py` | Proposal v2 始终为不可变 `PROPOSED`，绑定 candidate/DSL/Context/evidence/target/catalog/scope 并内容寻址持久化；确认权威已移至独立 receipt，foundation artifact authority、rolo-vis 和真实用户确认仍待接入 |
| FEAT-MAPPING-ADMISSION | PARTIAL | E2 | fixture CLI plus production operator/ACL/ledger-head SPI | `src/rolo/security/authority.py`; `src/rolo/dsl/admission.py`; `src/rolo/dsl/cli.py`; `src/rolo/dsl/service.py`; `src/rolo/mvp/probe_registration.py`; `src/rolo/targetd/dsl_service.py`; `src/rolo/targetd/service.py`; `src/rolo/releases/consumers.py`; `schemas/security/`; `schemas/rolo-dsl/v1/mapping-authority-command.json`; `schemas/rolo-dsl/v1/mapping-authority-pending.json`; `schemas/rolo-dsl/v1/mapping-authority-receipt.json` | `tests/test_mapping_admission.py`; `tests/test_dsl_cli.py`; `tests/test_dsl_service.py`; `tests/test_probe_registration.py`; `tests/test_targetd_dsl_service.py`; `tests/test_dsl_consumers.py` | production store/gate 要求外部签名 operator assertion、issuer-bound ACL、三用途 issuer/key/trust-root 隔离与 challenged fresh CAS head；pending 暴露双 ledger/anchor 不确定性，并在慢 verifier、anchor 和 callback 临界点重采可信时间，尾删/历史 head 重放/坏签名/重复 ACL/cancel guard/跨 expiry 均 fail-closed。Targetd/DSL/Release consumer 可注入 production gate，旧 unsigned targetd cancel 对 production store 拒绝；真实 IdP/JWKS/KMS、anchor、可信时钟、OS ACL/owner、signed cancel transport 与默认 production composition 尚需部署，因此 CLI fixture 绝非生产认证 |
| FEAT-CONTEXT-LAYER-DIGESTS | PARTIAL | E2 | target/runtime/surface/evidence digest and dirty evaluation | `src/rolo/dsl/context_digests.py`; `schemas/rolo-dsl/v1/context-change-report.json` | `tests/test_dsl_context_digests.py` | 分层变化判定已固定；真实 Bootstrap Probe 的增量重探与 UI 展示仍待接入 |
| FEAT-LANDERPI-ROS2-RUNTIME | PARTIAL | E3 | MentorPi ROS2 runtime evidence | `docs/validation/LANDERPI_ROS2_RUNTIME_DETECTION_20260905.md`; `docs/validation/LANDERPI_ROS2_RUNTIME_OBSERVATION_20260906.json`; `docs/validation/LANDERPI_ODOM_SOURCE_ANALYSIS_20260906.md`; `docs/validation/LANDERPI_ODOM_EKF_DIAGNOSTIC_20260906.md`; `src/rolo/targetd/ros2_runtime.py`; `src/rolo/mvp/trace_diagnostics.py`; `scripts/landerpi_odom_trace.py` | `tests/test_targetd_ros2_runtime.py`; `tests/test_landerpi_ros2_snapshot.py`; `tests/test_trace_diagnostics.py` | 已证实 ROS2 Humble、bringup、odom_publisher 和 EKF remap；诊断必须以 `ubuntu` 用户执行；已发现并修复 EKF yaw 融合、set_odom 时钟/类型崩溃和 bringup 环境依赖，仍需把环境校验、进程树清理和重复旋转纳入客户机 Certify |
| FEAT-LANDERPI-N7-R0-CANARY | PARTIAL | E3 | fixed `/odom` zero-motion formal targetd journey | `scripts/landerpi_n7_r0_canary.py`; `docs/validation/LANDERPI_N7_READONLY_PREFLIGHT_20260908.json`; `docs/validation/LANDERPI_N7_MOTION_GATE_RECHECK_20260909.json`; `docs/validation/landerpi_n6_formal_r0_20260909/` | `tests/test_landerpi_n7_r0_canary.py`; `tests/test_targetd_certify_adapter.py`; `tests/test_targetd_protocol.py`; `tests/test_targetd_ros2_runtime.py` | LIVE session `n7-r0-20260908T161029Z-7cda2170`：T3 1、Trace 1、正式 Certify 10；targetd CALL counter 11，cancel 后仍为 11；10 receipt/report/index digest 全部复核，raw 未持久化，同通道 cleanup residual 0/no fallback。2026-09-09 真机只读重检仍为 76%/约 13.1 GiB 可用，但 `operator_auth=FIXTURE`、6 个 cmd_vel publisher、2 个 direct-motor publisher、无已验证独立急停/现场操作员/实时 stop ack，实体运动继续 BLOCKED |

本轮 pytest 文件 allowlist 已补入 DSL API/service digest/BundlePlan/Mapping Admission/resolver/replay/release、
probe registration、LanderPi mapping runtime 和 N7 R0 canary 测试，避免新增边界因文件名未被收集而
产生假绿。收口完整回归为 `955 passed, 5 skipped, 2 warnings in 52.11s`；225 份 schema JSON、
79 份 Markdown、release-check、MVP gate、post-compiler replay、增量 Ruff 和 diff-check 均为 `PASS`。

经用户明确授权，LanderPi host 与 `MentorPi` 容器内经精确识别的 Rolo/RKB 工程目录、中间产物、
旧 trace/canary/probe 脚本、ROS `.bak-rolo` 备份和 Rolo uv editable cache 已不可恢复地删除；复核无
明确 Rolo/RKB 残留，未修改 `/home/pi/robot_pi` 或 ROS 当前文件。历史不可变
[N7 只读预检](../validation/LANDERPI_N7_READONLY_PREFLIGHT_20260908.json) 记录清理后根分区仍为 97%/约
1.9 GiB 可用；后续只读
[motion gate 重检](../validation/LANDERPI_N7_MOTION_GATE_RECHECK_20260909.json) 已观察到 76%/约 13.1 GiB
可用，持久容量门可重新判为通过。该变化不解除实体运动门：`/controller/cmd_vel` 仍有 6 个发布者，
`/ros_robot_controller/set_motor` 仍有 2 个发布者，且没有已验证的独立急停、现场操作员、安全区或
exact-call stop acknowledgement。

R0 LIVE canary 继续只在 `/dev/shm` 容量/大小门和 exact-root cleanup 下使用临时 stage；四次历史
BLOCKED 尝试、direct CALL [PASS artifact](../validation/LANDERPI_N3_N6_N7_R0_CANARY_20260908.json) 和
新的正式 [N6 R0 证据目录](../validation/landerpi_n6_formal_r0_20260909/) 均保留。正式 run 的 10 个
CallReceipt、report 与 index 已逐项复核，远端 stage 再次确认不存在。用于继续调试的 key-only SSH
公钥授权暂时保留；没有使用密码，也未执行运动。

## 可信度边界

- Agent 的自然语言、候选工具和计划不是事实；Rolo 的 descriptor、session、digest、结果
  artifact 和 Conformance 才是可审计边界。
- Codex 已知的目标 OS/Middleware CLI 不重复包装；Rolo 只负责固定 argv、目标绑定、环境边界和证据。
- 当 Codex 无法稳定调用目标 OS/Middleware 或 application 的底层接口时，才值得新增 Adapter bundle；新增能力必须
  经过 bounded Probe、TargetEvidence、Adapter bundle 和独立 Conformance。
- 真实目标机验证当前覆盖 mentorpi 的当前 OS/Middleware 只读证据；provider-specific coverage
  仍不是产品级全平台承诺。写操作、驱动变更和
  物理行为验收仍需单独授权。

## 同步规则

修改 `src/rolo/targets/`、`src/rolo/agent_tools/`、目标证据、公共 CLI 或 schema 时，必须
更新本表和对应测试路径。只要证据等级没有提升，不得把 `PARTIAL` 自动改成 `STABLE`。
